{
  "name": "voiceshield-ai",
  "install": "pip install -r requirements.txt && cd frontend && npm install",
  "build": "cd frontend && npm run build && mkdir -p backend/static && cp -r frontend/dist/* backend/static/",
  "preview": "python3 -m uvicorn backend.main:app --host 0.0.0.0 --port ${PORT:-8000}",
  "port": 8000
}
